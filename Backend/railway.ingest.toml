# Optional: attach this as a second Railway service from the same repo.
# Start command: python scripts/ingest_loop.py

[build]
builder = "DOCKERFILE"
dockerfilePath = "Backend/Dockerfile"

[deploy]
startCommand = "python scripts/ingest_loop.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
